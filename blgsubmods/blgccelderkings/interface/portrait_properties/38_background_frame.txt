# p38 Background frames
38 = {
	0 = { # blank
		factor = 5
		modifier = {
			factor = 100
			NOR = {
				is_ruler = yes
				spouse = { is_ruler = yes }
			}
		}
		modifier = {
			factor = 100
			OR = {
				NOT = { host = { at_location = ROOT } }
				in_command = yes
				is_indisposed = yes
			}
		}
	}
	1 = { # Wooden window
		factor = 2
		trigger = {
			is_nomadic = no
		}
	}
	2 = { # Stone window
		factor = 2
		trigger = {
			capital_holding = {
				NOR = {
					holding_type = tribal
					holding_type = nomad
				}
			}
		}
	}
	3 = { # Stone window
		factor = 2
		trigger = {
			capital_holding = {
				NOR = {
					holding_type = tribal
					holding_type = nomad
				}
			}
		}
	}
	4 = { # Nomad window
		factor = 2
		trigger = {
			capital_scope = {
				region = nirn_akavir
			}
		}
	}
	5 = { # Stone window
		factor = 2
		trigger = {
			capital_holding = {
				NOR = {
					holding_type = tribal
					holding_type = nomad
				}
			}
		}
	}
	6 = { # Glass window
		factor = 1
		trigger = {
			capital_holding = {
				NOR = {
					holding_type = tribal
					holding_type = nomad
				}
			}
			OR = {
				higher_real_tier_than = count
				spouse = { higher_real_tier_than = count }
			}
		}
	}
	7 = { # Grated window
		factor = 1
		trigger = {
			capital_holding = {
				NOR = {
					holding_type = tribal
					holding_type = nomad
				}
			}
			capital_scope = {
				NOT = { culture_group = nedic }
			}
			OR = {
				higher_real_tier_than = count
				spouse = { higher_real_tier_than = count }
			}
		}
	}
}
