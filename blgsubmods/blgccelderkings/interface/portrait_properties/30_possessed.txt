#p30 eye effects
30 = {
	0 = { # blank
		factor = 1
		modifier = {
			factor = 100
			OR = {
				has_game_rule = { name = blg_traits value = notraits }
				has_game_rule = { name = blg_traits value = nodisease }
				has_game_rule = { name = blg_traits value = off }
			}
		}
	}
	1 = { #Demon Child (fantasy only)
		factor = 100
		trigger = {
			OR = {
				trait = lich
				#has_character_modifier = pureblood_vampire
				has_character_flag = stage_4_vampire
			}
			OR = {
				trait = lich
				trait = known_vampire
				trait = known_vampire_benevolent
			}
		}
	}
	2 = { # possessed
		factor = 100
		trigger = {
			trait = possessed
			is_alive = yes
			NOT = { has_game_rule = { name = blg_traits value = nolunatic } }
		}
	}
	3 = { # wolfblooded
		factor = 100
		trigger = {
			OR = {
				trait = known_vampire
				trait = known_vampire_benevolent
				has_character_flag = demon_child
				has_character_flag = demon_child_fake
				has_character_flag = demon_child_non_pagan
			}
		}
	}
	4 = { # incapable
		factor = 100
		trigger = {
			OR = {
				trait = incapable
				trait = sick_incapable
			}
			is_alive = yes
		}
	}
	5 = { # lunatic
		factor = 100
		trigger = {
			trait = lunatic
			is_alive = yes
			NOT = { has_game_rule = { name = blg_traits value = nolunatic } }
		}
	}
	6 = { # depressed
		factor = 100
		trigger = {
			trait = depressed
			is_alive = yes
		}
	}
	7 = { # stressed
		factor = 100
		trigger = {
			trait = stressed
			is_alive = yes
		}
	}
}
