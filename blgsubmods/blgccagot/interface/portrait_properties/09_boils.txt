# p9 boils
9 = {
	0 = { # blank
		factor = 1
	}

	1 = { #Undead Male
		factor = 100
		trigger = {
			trait = undead
			is_female = no
		}
	}

	2 = { #Undead Female
		factor = 100
		trigger = {
			trait = undead
			is_female = yes
		}
	}

	3 = { # Gray Plague
		factor = 100
		trigger = {
			OR = {
				trait = has_greyscale
				trait = has_grey_plague
			}
			NOT = { has_game_rule = { name = blg_disease value = off } }
		}
	}

	4 = { # Gray Scar
		factor = 100
		trigger = {
			trait = greyscale_scarred
			NOT = { has_game_rule = { name = blg_disease value = off } }
		}
	}

	5 = { # bubonic plague
		factor = 100
		trigger = {
			OR = {
				trait = has_bubonic_plague
				character = 4301
			}

			blg_disease_rule = yes
		}
	}

	6 = { # boils
		factor = 100
		trigger = {
			OR = {
				trait = has_small_pox
				trait = has_red_death
			}

			blg_disease_rule = yes
		}
	}

	7 = { # red dots
		factor = 100
		trigger = {
			OR = {
				trait = has_measles
				trait = has_typhus
				trait = rash
				trait = has_butterfly_fever
			}

			blg_disease_rule = yes
		}
	}

	8 = { # ugly mole
		factor = 99
		trigger = {
			trait = ugly
			NOT = { has_portrait_property = { layer = 9 index = 9 } }
			blg_traits_rule = yes
		}
	}

	9 = { # ugly monobrow
		factor = 99
		trigger = {
			trait = ugly
			NOT = { has_portrait_property = { layer = 9 index = 8 } }
			blg_traits_rule = yes
		}

		modifier = {
			factor = 100
			culture = ibbenese
		}
	}
}
